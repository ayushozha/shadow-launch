// Deprecated in v2. The new results page no longer renders a winner banner;
// use VerdictList + PersonaDebateSummary. Empty shim retained for safety.
export default function VerdictBanner(): null {
  return null;
}
